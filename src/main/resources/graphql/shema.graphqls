type Query {
    books : [Book]
    bookById(id: Int): Book
    bookByName(name: String): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    name: String
}